## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `go build ./...` and `go vet ./...` pass
- [ ] `go test ./...` passes
- [ ] If `web/src` changed: ran `npm --prefix web install && npm --prefix web run build` and committed the updated `internal/uiserver/dist`
- [ ] Updated README/CONTRIBUTING if user-facing behavior changed

## Related issues

<!-- Fixes #123 -->
