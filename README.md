\# prompt-diff



Treat system prompts like source code.

Track prompt history across git commits, visually diff token usage \& estimated costs, and benchmark model responses side-by-side with a zero-config local UI.

